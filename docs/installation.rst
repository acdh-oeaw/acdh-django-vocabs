============
Installation
============

At the command line::

    $ easy_install acdh-django-vocabs

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv acdh-django-vocabs
    $ pip install acdh-django-vocabs
